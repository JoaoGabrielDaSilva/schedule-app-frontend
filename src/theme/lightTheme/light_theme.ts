import { default_theme } from "../defaultTheme";

export const ligh_theme = {
  ...default_theme,
  background: {
    primary: '#FFFFFF'
  },
  color: {
    ...default_theme.color,
    heading: '#000000',
    text: '#372B2B'
  },
}